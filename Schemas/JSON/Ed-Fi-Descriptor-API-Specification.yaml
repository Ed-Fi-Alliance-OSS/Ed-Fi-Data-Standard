info:
  title: Ed-Fi Descriptors API Specification
  description: "The Ed-Fi ODS / API enables applications to read and write education data stored in an Ed-Fi ODS through a secure REST interface. \n***\n > *Note: Consumers of ODS / API information should sanitize all data for display and storage. The ODS / API provides reasonable safeguards against cross-site scripting attacks and other malicious content, but the platform does not and cannot guarantee that the data it contains is free of all potentially harmful content.* \n***\n"
  version: '6.0.0'
openapi: 3.0.4
components:
  schemas:
    edFi_absenceEventCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_academicHonorCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_academicSubjectDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_accommodationDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_accountTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_accreditationStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_achievementCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_additionalCreditTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_addressTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_administrationEnvironmentDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_administrativeFundingControlDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_aidTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_ancestryEthnicOriginDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_applicationEventResultDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_applicationEventTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_applicationSourceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_applicationStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_assessmentCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_assessmentIdentificationSystemDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_assessmentItemCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_assessmentItemResultDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_assessmentPeriodDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_assessmentReportingMethodDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_assignmentLateStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_attemptStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_attendanceEventCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_backgroundCheckStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_backgroundCheckTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_barrierToInternetAccessInResidenceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_behaviorDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_busRouteDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_calendarEventDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_calendarTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_candidateCharacteristicDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_candidateIdentificationSystemDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_careerPathwayDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_certificationExamStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_certificationExamTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_certificationFieldDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_certificationLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_certificationRouteDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_certificationStandardDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_charterApprovalAgencyTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_charterStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_citizenshipStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_classroomPositionDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_cohortScopeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_cohortTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_cohortYearTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_competencyLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_contactIdentificationSystemDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_contentClassDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_continuationOfServicesReasonDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_costRateDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_coteachingStyleObservedDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_countryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_courseAttemptResultDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_courseDefinedByDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_courseGPAApplicabilityDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_courseIdentificationSystemDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_courseLevelCharacteristicDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_courseRepeatCodeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_credentialEventTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_credentialFieldDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_credentialStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_credentialTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_creditCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_creditTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_crisisTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_cteProgramServiceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_curriculumUsedDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_degreeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_deliveryMethodDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_diagnosisDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_diplomaLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_diplomaTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_disabilityDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_disabilityDesignationDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_disabilityDeterminationSourceTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_disciplineActionLengthDifferenceReasonDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_disciplineDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_disciplineIncidentParticipationCodeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_displacedStudentStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_dualCreditInstitutionDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_dualCreditTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_economicDisadvantageDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_educationalEnvironmentDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_educationOrganizationAssociationTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_educationOrganizationCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_educationOrganizationIdentificationSystemDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_educationPlanDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_educatorRoleDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_electronicMailTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_eligibilityDelayReasonDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_eligibilityEvaluationTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_employmentStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_englishLanguageExamDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_enrollmentTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_entryGradeLevelReasonDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_entryTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_eppDegreeTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_eppProgramPathwayDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_evaluationDelayReasonDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_evaluationElementRatingLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_evaluationPeriodDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_evaluationRatingLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_evaluationRatingStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_evaluationTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_eventCircumstanceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_exitWithdrawTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_federalLocaleCodeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_fieldworkTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_financialCollectionDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_fundingSourceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_goalTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_gradebookEntryTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_gradeLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_gradePointAverageTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_gradeTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_gradingPeriodDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_graduationPlanTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_gunFreeSchoolsActReportingStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_hireStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_hiringSourceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_homelessPrimaryNighttimeResidenceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_homelessProgramServiceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_ideaPartDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_identificationDocumentUseDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_immunizationTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_incidentLocationDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_indicatorDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_indicatorGroupDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_indicatorLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_institutionTelephoneNumberTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_instructionalSettingDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_interactivityStyleDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_internetAccessDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_internetAccessTypeInResidenceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_internetPerformanceInResidenceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_interventionClassDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_interventionEffectivenessRatingDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_languageDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_languageInstructionProgramServiceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_languageUseDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_learningStandardCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_learningStandardEquivalenceStrengthDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_learningStandardScopeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_lengthOfContractDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_levelOfEducationDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_licenseStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_licenseTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_limitedEnglishProficiencyDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_localeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_localEducationAgencyCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_magnetSpecialProgramEmphasisSchoolDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_mediumOfInstructionDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_methodCreditEarnedDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_migrantEducationProgramServiceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_modelEntityDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_monitoredDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_neglectedOrDelinquentProgramDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_neglectedOrDelinquentProgramServiceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_networkPurposeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_nonMedicalImmunizationExemptionDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_objectiveRatingLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_openStaffPositionEventStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_openStaffPositionEventTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_openStaffPositionReasonDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_operationalStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_otherNameTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_participationDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_participationStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_pathMilestoneStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_pathMilestoneTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_pathPhaseStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_performanceBaseConversionDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_performanceEvaluationRatingLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_performanceEvaluationTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_performanceLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_personalInformationVerificationDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_platformTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_populationServedDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_postingResultDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_postSecondaryEventCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_postSecondaryInstitutionLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_previousCareerDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_primaryLearningDeviceAccessDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_primaryLearningDeviceAwayFromSchoolDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_primaryLearningDeviceProviderDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_professionalDevelopmentOfferedByDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_proficiencyDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_programAssignmentDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_programCharacteristicDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_programEvaluationPeriodDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_programEvaluationTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_programSponsorDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_programTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_progressDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_progressLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_providerCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_providerProfitabilityDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_providerStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_publicationStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_quantitativeMeasureDatatypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_quantitativeMeasureTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_questionFormDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_raceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_ratingLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_reasonExitedDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_reasonNotTestedDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_recognitionTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_recruitmentEventAttendeeTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_recruitmentEventTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_relationDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_repeatIdentifierDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_reporterDescriptionDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_reportingTagDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_residencyStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_responseIndicatorDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_responsibilityDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_restraintEventReasonDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_resultDatatypeTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_retestIndicatorDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_rubricRatingLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_salaryTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_schoolCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_schoolChoiceBasisDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_schoolChoiceImplementStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_schoolFoodServiceProgramServiceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_schoolTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_section504DisabilityDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_sectionCharacteristicDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_sectionTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_separationDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_separationReasonDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_serviceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_sexDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_sourceSystemDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_specialEducationExitReasonDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_specialEducationProgramServiceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_specialEducationSettingDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_staffClassificationDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_staffIdentificationSystemDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_staffLeaveEventCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_staffToCandidateRelationshipDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_stateAbbreviationDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_studentCharacteristicDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_studentIdentificationSystemDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_submissionStatusDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_supporterMilitaryConnectionDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_surveyCategoryDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_surveyLevelDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_teachingCredentialBasisDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_teachingCredentialDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_technicalSkillsAssessmentDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_telephoneNumberTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_termDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_titleIPartAParticipantDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_titleIPartAProgramServiceDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_titleIPartASchoolDesignationDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_transportationPublicExpenseEligibilityTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_transportationTypeDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_travelDayofWeekDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_travelDirectionDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_tribalAffiliationDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_visaDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_weaponDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    edFi_withdrawReasonDescriptor:
      required:
      - codeValue
      - namespace
      - shortDescription
      type: object
      properties:
        id:
          type: string
          description: ''
        codeValue:
          maxLength: 50
          type: string
          description: A code or abbreviation that is used to refer to the descriptor.
          x-Ed-Fi-isIdentity: true
        namespace:
          maxLength: 255
          type: string
          description: A globally unique namespace that identifies this descriptor set. Author is strongly encouraged to use the Universal Resource Identifier (http, ftp, file, etc.) for the source of the descriptor definition. Best practice is for this source to be the descriptor file itself, so that it can be machine-readable and be fetched in real-time, if necessary.
          x-Ed-Fi-isIdentity: true
        description:
          maxLength: 1024
          type: string
          description: The description of the descriptor.
          x-nullable: true
        effectiveBeginDate:
          type: string
          description: The beginning date of the period when the descriptor is in effect. If omitted, the default is immediate effectiveness.
          format: date
          x-nullable: true
        effectiveEndDate:
          type: string
          description: The end date of the period when the descriptor is in effect.
          format: date
          x-nullable: true
        shortDescription:
          maxLength: 75
          type: string
          description: A shortened description for the descriptor.
    link:
      type: object
      properties:
        rel:
          type: string
          description: Describes the nature of the relationship to the referenced resource.
        href:
          type: string
          description: The URL to the related resource.
